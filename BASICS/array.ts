const superHeros: string[] = []
const superPower: number[] = []
const superVillan: Array<number> = []

type User = {
    name:string,
    isActive : boolean
}

const allUsers : User[] = []

const MLModels:number[][]=[
    [255,255,255],
    []
]

superHeros.push("spaiderman")
superPower.push(2)

allUsers.push({name:"",isActive:true}) 